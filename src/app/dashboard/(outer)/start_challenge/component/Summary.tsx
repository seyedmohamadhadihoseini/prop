
export default function Summary({ server, balance, platform, leverage }: { server: string, balance: string, platform: string, leverage: string }) {

    return <div>
        <h3>Selected challenge specifications</h3>
        
    </div>
}